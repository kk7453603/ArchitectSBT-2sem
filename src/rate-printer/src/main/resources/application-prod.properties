# Production profile
# ZooKeeper via Docker network or external host (overridden by env var if set)
spring.cloud.zookeeper.connect-string=${ZOOKEEPER_CONNECT_STRING:zookeeper:2181}

# Reduced logging
logging.level.com.example.rateprinter=INFO
logging.level.root=WARN
